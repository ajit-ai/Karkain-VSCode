// Real project layout as generated by Karkain 1.1.0 `pkg init`
// (flat karkain.toml keys, src/main.kark, tests/*_test.kark).
name = "sample"
version = "0.1.0"
description = "A Karkain project"
license = "MIT"
targets = ["native"]
